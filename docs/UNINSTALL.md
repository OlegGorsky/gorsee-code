# Uninstall

Remove the npm package:

```bash
npm uninstall -g @gorsee/code
```

Project-local data lives in:

```text
.gorsee-code/
gorsee-code.toml
```

User-level data, when introduced by future releases, should live under the
platform-specific config/cache directories and will be documented here.
